type Mutation {
  register(
    firstName: String!
    lastName: String!
    address: String!
    phone: String
    email: String!
    password: String!
  ): AuthPayload!
  login(email: String!, password: String!): AuthPayload!
}
